#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
SKILL_DIR="$(cd "${SCRIPT_DIR}/.." && pwd)"

cat <<EOF
jazz-loading-subscriptions documents loading depth and subscription design.

Read:
- ${SKILL_DIR}/SKILL.md
- ${SKILL_DIR}/README.md
- ${SKILL_DIR}/references/loading-subscriptions/
EOF
