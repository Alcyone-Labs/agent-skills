# Runtime Layout

- `runtime/browser/lightpanda`: provisioned browser binary.
- `runtime/state/lightpanda.pid`: current server process ID.
- `runtime/state/lightpanda.log`: server log output.

Provisioning runs via `scripts/provision-runtime.sh` during install/update/reset.
