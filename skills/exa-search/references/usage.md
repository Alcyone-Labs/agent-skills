# Usage Notes

- Run `exa-set-key YOUR_API_KEY` once to store credentials.
- Prefer `exa-search` for general web queries.
- Use `exa-code` when searching implementation examples.
- Use `exa-company` for company-focused research.
- Use `exa-crawl` to extract content from specific URLs.
